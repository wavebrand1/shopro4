{% extends 'admin/layout.html.twig' %}{% block title %}{{ language.id?'Edycja języka':'Nowy język' }} — Shopro 4.0{% endblock %}{% block body %}
<div class="modern-page-heading"><div><p class="modern-kicker">KONFIGURACJA</p><h1>{{ language.id?'Edytuj język':'Dodaj język' }}</h1><p>Pola odpowiadają konfiguracji języka w Shopro Legacy.</p></div><a class="modern-button" href="{{ path('admin_language_index') }}">← Wróć</a></div>{{ form_start(form,{attr:{class:'admin-form'}}) }}<section class="modern-card"><div class="cms-card-body"><div class="component-fields">{{ form_row(form.name) }}{{ form_row(form.code) }}{{ form_row(form.author) }}{{ form_row(form.subdomain) }}</div>{{ form_row(form.direction) }}{{ form_row(form.active) }}</div></section><div class="cms-sticky-actions"><button class="modern-button modern-button--primary">Zapisz język</button></div>{{ form_end(form) }}{% endblock %}

<section class="site-logos"><div class="site-container"><p>{{ block.text|default('') }}</p><div>{% for item in block.items|default([]) %}<span>{{ item.name|default('') }}</span>{% endfor %}</div></div></section>

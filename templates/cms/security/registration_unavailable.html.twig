{% extends 'cms/layout.html.twig' %}{% block title %}{{ shopro_trans('site_registration.unavailable') }}{% endblock %}{% block page_content %}<section class="site-section"><div class="site-container"><div class="modern-login-card site-account-card"><h1>{{ shopro_trans('site_registration.unavailable') }}</h1><p>{{ shopro_trans('site_registration.limit_reached') }}</p><a class="modern-button" href="{{ path('app_home') }}">{{ shopro_trans('front.back_home') }}</a></div></div></section>{% endblock %}

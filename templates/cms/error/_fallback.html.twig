<section class="site-error-page">
    <div class="site-container site-error-page__inner">
        <div class="site-error-page__code">404</div>
        <span class="site-kicker">{{ shopro_trans('error.kicker') }}</span>
        <h1>{{ shopro_trans('error.404_title') }}</h1>
        <p>{{ shopro_trans('error.404_lead') }}</p>
        <a class="site-button" href="{{ path('app_home') }}">← {{ shopro_trans('error.back_home') }}</a>
    </div>
</section>
